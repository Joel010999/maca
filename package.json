{
  "name": "malestyle-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend && npx --prefix backend prisma generate --schema=backend/prisma/schema.prisma && npm run build --prefix backend",
    "start": "npx prisma db push --accept-data-loss --schema=backend/prisma/schema.prisma && node backend/dist/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}

{
  "name": "malestyle-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend && cd backend && npx prisma generate && npm run build",
    "start": "cd backend && npx prisma db push --accept-data-loss && node dist/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
